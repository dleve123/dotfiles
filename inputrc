# vimify the command line
set editing-mode vi

# display ambiguous matches on first tab
set show-all-if-ambiguous on

# tab out matches in a case insensitive way
set completion-ignore-case on

# hit control-l in bash and irb to clear the console
set keymap vi-command
"\C-l": clear-screen

#FIXME: can no longer tab out variable names in ruby and rails consoles...
